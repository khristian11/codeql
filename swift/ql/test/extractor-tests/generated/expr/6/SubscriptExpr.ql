// generated by codegen/codegen.py
import codeql.swift.elements
import TestUtils

from
  SubscriptExpr x, string isUnknown, string hasDirectToStorageSemantics,
  string hasDirectToImplementationSemantics, string hasOrdinarySemantics
where
  toBeTested(x) and
  (if x.isUnknown() then isUnknown = "isUnknown" else isUnknown = "not isUnknown") and
  (
    if x.hasDirectToStorageSemantics()
    then hasDirectToStorageSemantics = "hasDirectToStorageSemantics"
    else hasDirectToStorageSemantics = "not hasDirectToStorageSemantics"
  ) and
  (
    if x.hasDirectToImplementationSemantics()
    then hasDirectToImplementationSemantics = "hasDirectToImplementationSemantics"
    else hasDirectToImplementationSemantics = "not hasDirectToImplementationSemantics"
  ) and
  if x.hasOrdinarySemantics()
  then hasOrdinarySemantics = "hasOrdinarySemantics"
  else hasOrdinarySemantics = "not hasOrdinarySemantics"
select x, isUnknown, x.getBaseExpr(), x.getMember(), hasDirectToStorageSemantics,
  hasDirectToImplementationSemantics, hasOrdinarySemantics
