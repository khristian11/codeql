// generated by codegen/codegen.py
import codeql.swift.elements
import TestUtils

from
  AccessorDecl x, string isUnknown, string isGetter, string isSetter, string isWillSet,
  string isDidSet
where
  toBeTested(x) and
  (if x.isUnknown() then isUnknown = "isUnknown" else isUnknown = "not isUnknown") and
  (if x.isGetter() then isGetter = "isGetter" else isGetter = "not isGetter") and
  (if x.isSetter() then isSetter = "isSetter" else isSetter = "not isSetter") and
  (if x.isWillSet() then isWillSet = "isWillSet" else isWillSet = "not isWillSet") and
  if x.isDidSet() then isDidSet = "isDidSet" else isDidSet = "not isDidSet"
select x, isUnknown, x.getInterfaceType(), x.getName(), isGetter, isSetter, isWillSet, isDidSet
